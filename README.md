Để chạy project này,đầu tiên bạn cầu truyền vào các tham số trong file env
Chạy dòng lệnh : pip install -r requirements.txt
Chạy dòng lệnh : python server.py
Chạy dòng lệnh : streamlit run chat_app.py
